# MCP BigQuery Server Environment Configuration

# Google Cloud Project ID (required)
# Set this to your Google Cloud project ID
PROJECT_ID=your-google-cloud-project-id

# Google Cloud Authentication (optional)
# If you want to use service account authentication instead of Application Default Credentials,
# uncomment and set the path to your service account JSON file:
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# Note: When using Application Default Credentials (recommended approach),
# you don't need to set GOOGLE_APPLICATION_CREDENTIALS.
# Just run 'make auth' to authenticate with Google Cloud.

# Optional: Default dataset
DEFAULT_DATASET=your-default-dataset

# Optional: Query timeout (in seconds)
QUERY_TIMEOUT=300

# Optional: Maximum results per query
MAX_RESULTS=10000 