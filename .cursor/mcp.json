{
    "mcpServers": {
        "mcp_bigquery_server": {
            "command": "uv",
            "args": [
                "run",
                "./src/mcp_bigquery_server.py"
            ],
            "description": "Execute SQL queries, list datasets/tables, and get table schemas from BigQuery"
        }
    }
}